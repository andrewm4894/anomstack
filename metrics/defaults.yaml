table_key: "tmp.tmpmetrics"
train_sql: >
  select
    *
  from
    {{ table_key }}
  where
    batch = '{{ batch }}'
  order by timestamp desc
  limit 1000;
score_sql: >
  select
    *
  from
    {{ table_key }}
  where
    batch = '{{ batch }}'
  order by timestamp desc
  limit 100;