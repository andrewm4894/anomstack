# Local development environment for ALL metric batches
# This profile configures the dashboard to use the comprehensive database with:
# python_ingest_simple, netdata, posthog, yfinance, and currency metric batches

# Database configuration  
ANOMSTACK_DUCKDB_PATH=tmpdata/anomstack-all.db

# Development settings
ANOMSTACK_DASHBOARD_DEBUG=true

# Enable examples to see all batches
ANOMSTACK_IGNORE_EXAMPLES=no

# All batch configurations use defaults from their respective YAML files 